package com.demo.store.exceptions;

/**
 * No need to make this a checked exception or add body, so extending RuntimeException instead of Exception!
 */
public class CartNotFoundException extends RuntimeException {

}
